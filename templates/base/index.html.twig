{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}

    <style>
        .impact { font-family: Impact, Haettenschweiler, 'Arial Narrow Bold', sans-serif
        };
    </style>

    

<div class="">
    <img src="assets/Baniere_mission_sport.png" alt="">
</div>

<div class="grey p-3 z-0">
    <h1 class="text-center text-2xl font-bold">Produit phare</h1>
</div>

<!--Container des Best Seller -->
<div class=" mx-auto grey p-3 flex flex-wrap justify-center ">
    <a href="{{ path('produitPage', {'id': 5}) }}">
    <div  class="w-72 mx-auto rounded-md sm:w-full md:w-full lg:w-1/3 xl:w-1/4">
        <img class="w-30 h-80 mx-auto object-cover rounded-md transition hover:scale-110 " src="assets/programme.jpg" alt="image produit" />
        <div class="p-4 grey items-center justify-between mb-4">
            <div class="">
                <div class="text-center">
                    <h5 class="text-xl font-semibold mb-2">Introduction musculation</h5>
                    <p class="text-gray-700 mb-4 ">Initiation à la musculation en 4 semaines.</p>
                    <p class="text-gray-700 mb-4 font-bold">15.99€.</p>
                    
                    <div class="flex items-center justify-center">
                        {% if is_granted('IS_AUTHENTICATED_FULLY')%}
                        <a href="{{path('gestionPanier',{'id':5,'action':'addPanier'})}}" class="bg-black impact text-white py-2 px-2 rounded-md transition hover:scale-110 ">Ajouter au panier</a>
                        {% else %}
                        <a href="login" class="bg-black impact text-white py-2 px-2 rounded-md transition hover:scale-110 ">Ajouter au panier</a>
                        {% endif %}                            
                        
                    </div>
                </div>
            </div>
        </div>
    </div>
    
    <a href="{{ path('produitPage', {'id': 2}) }}">
    <div class="w-72 mx-auto rounded-md sm:w-full md:w-full lg:w-1/3 xl:w-1/4">
        <img class="w-30 h-80 mx-auto object-cover rounded-md transition hover:scale-110 " src="assets/elastique 10kg.jpg" alt="image produit" />
        <div class="p-4 grey ">
            <div class="flex items-center justify-center mb-4">
                <div class="text-center">
                    <h5 class="text-xl font-semibold mb-2">Elastique 15 KG</h5>
                    <p class="text-gray-700 mb-4">Elastique avec une force de 15 KG Maximum</p>
                    <p class="text-gray-700 mb-4 font-bold">15 €.</p>
                    
                    <div class="flex items-center justify-center">
                        {% if is_granted('IS_AUTHENTICATED_FULLY')%}
                            <a href="{{path('gestionPanier',{'id':2,'action':'addPanier'})}}" class="bg-black impact text-white py-2 px-2 rounded-md transition hover:scale-110 ">Ajouter au panier</a>
                        {% else %}
                        <a href="login" class="bg-black impact text-white py-2 px-2 rounded-md transition hover:scale-110 ">Ajouter au panier</a>
                        {% endif %}
                    
                    </div>
                </div>
            </div>
        </div>
    </div>
    </a>
    <a href="{{ path('produitPage', {'id': 6}) }}">
    <div class="w-72 mx-auto rounded-md sm:w-full md:w-full lg:w-1/3 xl:w-1/4">
        <img class="w-30 h-80 mx-auto object-cover rounded-md transition hover:scale-110 " src="assets/programme.jpg" alt="image produit" />

        <div class="p-4 grey ">
            <div class="flex items-center justify-center mb-4">
                <div class="text-center">
                    <h5 class="text-xl font-semibold mb-2">Programme Misssti Soir</h5>
                    <p class="text-gray-700 mb-4">Programme personnalisé complet.</p>
                    <p class="text-gray-700 mb-4 font-bold">45.99€.</p>
                    
                    <div class="flex items-center justify-center">
                        {% if is_granted('IS_AUTHENTICATED_FULLY')%}
                            <a href="{{path('gestionPanier',{'id':6,'action':'addPanier'})}}" class="bg-black impact text-white py-2 px-2 rounded-md transition hover:scale-110 ">Ajouter au panier</a>
                        {% else %}
                            <a href="login" class="bg-black impact text-white py-2 px-2 rounded-md transition hover:scale-110 ">Ajouter au panier</a>
                        {% endif %}                            

                    </div>
                </div>
            </div>
        </div>
    </div>
    </a>

    







</div>


{% endblock %}

